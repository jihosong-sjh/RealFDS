# Feature Specification: 실시간 시스템 대시보드

**Feature Branch**: `004-dashboard-realtime`
**Created**: 2025-11-10
**Status**: Draft
**Input**: User description: "004-dashboard-realtime 한국어로 알려줘"

---

**📝 문서화 언어 원칙 (Constitution VI)**:
- 모든 섹션은 **한국어**로 작성합니다
- 사용자 시나리오, 요구사항, 성공 기준 등 모든 내용은 한국어로 기술합니다
- 기술 용어는 영어 병기 가능 (예: "이벤트 시간(event-time)")
- 예시 코드/스키마는 영어 사용 가능하나 설명은 한국어로 작성합니다

---

## User Scenarios & Testing *(mandatory)*

### User Story 1 - 서비스 상태 실시간 모니터링 (Priority: P1)

운영자가 5개 마이크로서비스(transaction-generator, fraud-detector, alert-service, websocket-gateway, frontend-dashboard)의 상태를 실시간으로 확인하여 장애를 조기에 발견하는 기능입니다.

**Why this priority**: 서비스 장애는 즉각적인 대응이 필요하며, 장애 감지 시간이 길어질수록 시스템 전체에 영향을 미칩니다. 이는 대시보드의 가장 핵심적인 기능으로, 운영자가 시스템 건강도를 가장 먼저 확인하는 지표입니다.

**Independent Test**: 각 서비스의 Health Check 엔드포인트를 호출하고 대시보드에 상태 카드가 올바르게 표시되는지 확인합니다. 하나의 서비스를 중단시켰을 때 3초 이내에 빨간색 DOWN 상태로 전환되는지 테스트하여 독립적으로 검증 가능합니다.

**Acceptance Scenarios**:

1. **Given** 운영자가 대시보드에 처음 접속했을 때, **When** 페이지가 로딩되면, **Then** 5개 서비스의 현재 상태가 각각의 상태 카드에 표시됨

2. **Given** 모든 서비스가 정상 작동 중일 때, **When** 상태 카드를 확인하면, **Then** 각 서비스 카드가 녹색으로 표시되고 "UP" 라벨이 보임

3. **Given** alert-service가 중단되었을 때, **When** 3초 후 대시보드를 확인하면, **Then** 해당 서비스 카드가 빨간색으로 변경되고 "DOWN" 라벨이 표시됨

4. **Given** 서비스 상태 카드를 클릭했을 때, **When** 클릭하면, **Then** 해당 서비스의 상세 정보(메모리 사용량, 응답 시간)를 보여주는 모달이 열림

5. **Given** 서비스가 다시 정상화되었을 때, **When** 5초 후 대시보드를 확인하면, **Then** 해당 서비스 카드가 다시 녹색 "UP" 상태로 전환됨

---

### User Story 2 - 실시간 거래량 추이 확인 (Priority: P1)

운영자가 초당 거래 처리량(Transactions Per Second, TPS)을 실시간 시계열 차트로 확인하여 트래픽 패턴을 파악하고 시스템 부하 상태를 모니터링하는 기능입니다.

**Why this priority**: 거래량은 시스템 부하의 직접적인 지표이며, 급격한 증가는 리소스 부족이나 비정상적인 트래픽을 의미할 수 있습니다. 서비스 상태와 함께 시스템 건강도의 핵심 지표로, P1 우선순위로 구현해야 합니다.

**Independent Test**: 거래 생성기를 통해 초당 10개, 50개, 100개로 거래 생성 속도를 변경하면서 차트가 5초마다 새로운 데이터 포인트를 추가하고, 현재 TPS 수치가 정확하게 업데이트되는지 확인합니다. 차트의 시간 축이 자동으로 스크롤되며 부드러운 애니메이션이 유지되는지 테스트합니다.

**Acceptance Scenarios**:

1. **Given** 운영자가 대시보드에 접속했을 때, **When** 거래량 섹션을 확인하면, **Then** 최근 1시간의 TPS가 시계열 차트로 표시됨

2. **Given** 거래가 지속적으로 발생하고 있을 때, **When** 5초마다 차트를 확인하면, **Then** 새로운 데이터 포인트가 차트 우측에 부드럽게 추가되고 시간 축이 자동으로 이동함

3. **Given** 거래량이 급증했을 때(예: TPS가 50에서 150으로 증가), **When** 차트를 확인하면, **Then** 그래프가 상승하고 현재 TPS 수치가 큰 글씨로 강조 표시됨

4. **Given** 차트에 마우스 커서를 올렸을 때, **When** 특정 시점의 데이터 포인트를 가리키면, **Then** 해당 시각과 정확한 TPS 값이 툴팁으로 표시됨

5. **Given** 1시간 이상 대시보드를 사용했을 때, **When** 차트를 확인하면, **Then** 1시간 이전의 오래된 데이터는 자동으로 차트에서 제거되고 최근 1시간 데이터만 유지됨

6. **Given** 현재 TPS, 평균 TPS, 최대 TPS 카드를 확인할 때, **When** 5초마다 업데이트되면, **Then** 각 수치가 깜빡임 없이 자연스럽게 변경됨

---

### User Story 3 - 실시간 알림 발생률 추이 확인 (Priority: P2)

운영자가 분당 알림 발생 수를 실시간 차트로 확인하여 사기 탐지 패턴의 변화를 감지하고 알림 규칙의 효율성을 평가하는 기능입니다.

**Why this priority**: 알림 발생률은 사기 활동의 변화를 나타내는 중요한 지표이지만, 서비스 상태와 거래량보다는 부차적인 모니터링 지표입니다. P2 우선순위로 분류하며, P1 기능이 완성된 후 추가 구현합니다.

**Independent Test**: 사기 탐지 규칙의 임계값을 조정하여 알림 발생 빈도를 변경하고, 차트가 규칙별로 알림 수를 구분하여 표시하는지 확인합니다. 특정 규칙(예: HIGH_VALUE)의 범례를 클릭하여 해당 데이터를 숨기거나 표시할 수 있는지 테스트합니다.

**Acceptance Scenarios**:

1. **Given** 운영자가 대시보드에 접속했을 때, **When** 알림 발생률 섹션을 확인하면, **Then** 최근 1시간의 분당 알림 수가 시계열 차트로 표시됨

2. **Given** 알림이 지속적으로 발생하고 있을 때, **When** 5초마다 차트를 확인하면, **Then** 새로운 데이터 포인트가 차트에 실시간으로 추가됨

3. **Given** 알림 발생률이 급증했을 때(예: 분당 5개에서 20개로 증가), **When** 차트를 확인하면, **Then** 그래프 영역 색상이 주황색으로 변경되어 경고 상태임을 시각적으로 표시함

4. **Given** 규칙별 알림 비율을 확인하고 싶을 때, **When** 차트 범례에서 특정 규칙(예: FOREIGN_COUNTRY)을 클릭하면, **Then** 해당 규칙의 데이터만 차트에서 숨김 처리되고, 다시 클릭하면 표시됨

5. **Given** 현재 알림 발생률, 평균, 최대값 카드를 확인할 때, **When** 5초마다 업데이트되면, **Then** 각 수치가 자연스럽게 변경됨

6. **Given** HIGH_VALUE, FOREIGN_COUNTRY, HIGH_FREQUENCY 세 가지 규칙이 있을 때, **When** 차트를 확인하면, **Then** 각 규칙이 다른 색상으로 구분되어 스택 형태로 표시됨

---

### Edge Cases

- **서비스 응답 지연**: 서비스 Health Check 응답이 5초 이상 걸릴 경우, 해당 서비스를 DOWN으로 간주하고 빨간색으로 표시해야 합니다

- **네트워크 끊김**: 대시보드와 백엔드 간 WebSocket 연결이 끊어진 경우, 화면 상단에 "연결 끊김" 경고 배너를 표시하고 자동 재연결을 시도해야 합니다

- **데이터 없음**: 시스템 초기 시작 시 1시간 이상의 메트릭 데이터가 없을 경우, 차트에 "데이터 수집 중..." 메시지를 표시해야 합니다

- **급격한 값 변화**: TPS가 1초 만에 10배 이상 증가하는 경우, 차트의 Y축 범위를 자동으로 조정하여 모든 데이터가 보이도록 해야 합니다

- **브라우저 탭 비활성화**: 사용자가 다른 탭으로 전환한 후 대시보드 탭으로 돌아왔을 때, 누락된 데이터를 자동으로 백필(backfill)하여 차트에 반영해야 합니다

- **동시 다수 사용자**: 5명 이상의 사용자가 동시에 대시보드를 사용할 경우, 각 사용자의 화면이 독립적으로 업데이트되어야 하며 서로 간섭하지 않아야 합니다

## Requirements *(mandatory)*

### Functional Requirements

**작성 가이드라인**:
- 모든 요구사항은 **한국어**로 작성합니다 (Constitution VI)
- 각 요구사항은 측정 가능하고 검증 가능해야 합니다
- Constitution 위반 가능성이 있는 요구사항은 명시적으로 표시합니다

**Constitution 준수 검증** (각 FR 작성 시 확인):
- [x] 학습 우선: 실시간 메트릭 수집과 시각화 개념 학습에 기여
- [x] 단순함: 핵심 모니터링 기능에만 집중
- [x] 실시간 우선: 5초 이내 메트릭 업데이트 보장
- [x] 서비스 경계: 기존 3개 서비스를 모니터링하며 새로운 서비스 추가 없음
- [x] 품질 표준: 모든 요구사항이 테스트 가능하고 로깅 가능

---

#### 서비스 상태 모니터링

- **FR-001**: 시스템은 5개 마이크로서비스(transaction-generator, fraud-detector, alert-service, websocket-gateway, frontend-dashboard)의 Health Check 상태를 수집해야 합니다

- **FR-002**: 각 서비스의 상태는 "정상(UP)" 또는 "중단(DOWN)" 두 가지 중 하나로 분류되어야 합니다

- **FR-003**: 서비스 상태는 5초마다 자동으로 업데이트되어야 합니다

- **FR-004**: 서비스가 중단 상태로 전환된 경우, 감지 후 3초 이내에 사용자 인터페이스에 반영되어야 합니다

- **FR-005**: 각 서비스 상태 카드는 서비스 이름, 현재 상태(UP/DOWN), 마지막 확인 시간을 표시해야 합니다

- **FR-006**: 사용자가 서비스 상태 카드를 클릭하면, 해당 서비스의 상세 정보(메모리 사용량, 평균 응답 시간)를 보여주는 모달이 열려야 합니다

#### 실시간 거래량 메트릭

- **FR-007**: 시스템은 초당 거래 처리량(TPS: Transactions Per Second)을 실시간으로 수집해야 합니다

- **FR-008**: 거래량 데이터는 최근 1시간 동안의 기록을 유지해야 하며, 1시간 이전의 데이터는 자동으로 제거되어야 합니다

- **FR-009**: 거래량 차트는 시간을 X축, TPS를 Y축으로 하는 선 그래프(line chart)로 표시되어야 합니다

- **FR-010**: 거래량 메트릭은 5초마다 업데이트되어야 하며, 새로운 데이터 포인트가 차트 우측에 추가되어야 합니다

- **FR-011**: 현재 TPS, 평균 TPS, 최대 TPS 값을 숫자 형태로 표시해야 합니다

- **FR-012**: 사용자가 차트의 특정 시점에 마우스 커서를 올리면, 해당 시각과 정확한 TPS 값이 툴팁으로 표시되어야 합니다

#### 실시간 알림 발생률 메트릭

- **FR-013**: 시스템은 분당 알림 발생 수(Alerts Per Minute)를 실시간으로 수집해야 합니다

- **FR-014**: 알림 데이터는 최근 1시간 동안의 기록을 유지해야 하며, 1시간 이전의 데이터는 자동으로 제거되어야 합니다

- **FR-015**: 알림 발생률 차트는 시간을 X축, 분당 알림 수를 Y축으로 하는 영역 차트(area chart)로 표시되어야 합니다

- **FR-016**: 알림은 규칙 유형별로 분류되어야 하며, HIGH_VALUE, FOREIGN_COUNTRY, HIGH_FREQUENCY 세 가지 규칙을 지원해야 합니다

- **FR-017**: 차트는 각 규칙 유형을 다른 색상으로 구분하여 스택(stacked) 형태로 표시해야 합니다

- **FR-018**: 사용자가 차트 범례에서 특정 규칙을 클릭하면, 해당 규칙의 데이터를 차트에서 숨기거나 다시 표시할 수 있어야 합니다

- **FR-019**: 현재 알림 발생률, 평균 알림 발생률, 최대 알림 발생률 값을 숫자 형태로 표시해야 합니다

- **FR-020**: 알림 발생률이 평균의 2배를 초과하는 경우, 차트 영역 색상을 주황색으로 변경하여 경고 상태를 시각적으로 표시해야 합니다

#### 사용자 인터페이스 및 사용성

- **FR-021**: 차트는 데이터 업데이트 시 깜빡임이나 끊김 없이 부드럽게 전환되어야 합니다

- **FR-022**: 대시보드의 초기 로딩 시간은 2초를 초과하지 않아야 합니다

- **FR-023**: 메트릭 수치가 업데이트될 때 애니메이션 효과를 적용하여 변화를 명확하게 보여주어야 합니다

- **FR-024**: 백엔드와의 WebSocket 연결이 끊어진 경우, 화면 상단에 "연결 끊김" 경고 배너를 표시하고 자동 재연결을 시도해야 합니다

- **FR-025**: 시스템 초기 시작 시 1시간 이상의 메트릭 데이터가 없는 경우, 차트에 "데이터 수집 중..." 메시지를 표시해야 합니다

- **FR-026**: 차트의 Y축 범위는 현재 데이터 범위에 맞춰 자동으로 조정되어야 하며, 모든 데이터 포인트가 차트 내에 보이도록 해야 합니다

### Key Entities *(include if feature involves data)*

- **ServiceHealth (서비스 상태)**:
  - 서비스 이름(serviceName)
  - 현재 상태(status): UP 또는 DOWN
  - 마지막 확인 시간(lastChecked)
  - 평균 응답 시간(responseTime): 밀리초 단위
  - 메모리 사용량(memoryUsage): 메가바이트 단위

- **TransactionMetrics (거래량 메트릭)**:
  - 측정 시각(timestamp)
  - 초당 거래 수(tps)
  - 누적 거래 수(totalTransactions)

- **AlertMetrics (알림 메트릭)**:
  - 측정 시각(timestamp)
  - 분당 알림 수(alertsPerMinute)
  - 규칙별 알림 수(byRule): 각 규칙(HIGH_VALUE, FOREIGN_COUNTRY, HIGH_FREQUENCY)별 알림 발생 수

## Success Criteria *(mandatory)*

### Measurable Outcomes

- **SC-001**: 메트릭 업데이트가 평균 4초, 최대 5초 이내에 완료되어 사용자가 거의 실시간으로 데이터를 확인할 수 있습니다

- **SC-002**: 차트가 부드럽게 애니메이션되며 데이터 업데이트 시 끊김 현상이 발생하지 않습니다

- **SC-003**: 서비스가 중단되었을 때 평균 2초, 최대 3초 이내에 대시보드에 DOWN 상태가 표시되어 운영자가 즉시 장애를 인지할 수 있습니다

- **SC-004**: 대시보드가 2초 이내에 로딩 완료되어 사용자가 대기 시간 없이 모니터링을 시작할 수 있습니다

- **SC-005**: 1시간 동안 대시보드를 지속적으로 사용해도 성능 저하 없이 일정한 응답 속도를 유지합니다

- **SC-006**: 5명의 사용자가 동시에 대시보드를 사용해도 각 사용자의 화면이 독립적으로 정상 작동하며 서로 간섭하지 않습니다

- **SC-007**: 운영자가 서비스 장애를 발견하고 상세 정보를 확인하는 데 걸리는 시간이 평균 10초 이내입니다

- **SC-008**: 차트에서 특정 시점의 정확한 수치를 확인하는 데 2초 이내에 가능합니다(마우스 호버 후 툴팁 표시)

## 시스템 경계

### In Scope
✅ 5개 서비스의 Health Check 상태 모니터링
✅ 실시간 TPS(초당 거래 수) 수집 및 시각화
✅ 실시간 알림 발생률 수집 및 시각화
✅ 시계열 차트를 통한 1시간 추이 확인
✅ 서비스 상세 정보 조회(메모리, 응답 시간)
✅ 규칙별 알림 분류 및 필터링
✅ 실시간 데이터 푸시(WebSocket)

### Out of Scope
❌ 메트릭 데이터의 영구 저장(데이터베이스 저장)
❌ 1시간 이전의 과거 메트릭 조회 및 분석
❌ 알림 임계값 설정 및 알림 발송 기능
❌ 메트릭 데이터 내보내기(CSV, JSON 파일 다운로드)
❌ 사용자별 대시보드 레이아웃 커스터마이징
❌ 여러 개의 대시보드 화면 전환
❌ 사용자 인증 및 권한 관리
❌ 알림 이력 조회(이는 003-alert-history 기능에서 다룸)

## 제약사항 및 가정

### Constraints
- 메트릭 데이터는 메모리에만 저장되며, 최근 1시간 데이터만 유지합니다
- 동시 접속 사용자는 최대 5명으로 제한합니다
- 메트릭 데이터 크기는 1시간당 10MB를 초과하지 않습니다

### Assumptions
- 각 서비스의 Health Check 엔드포인트 응답 시간은 100ms 이내입니다
- 사용자의 브라우저는 WebSocket 프로토콜을 지원합니다
- 대시보드는 데스크톱 화면(최소 1280x720 해상도)에서 사용됩니다
- 거래 생성 속도는 초당 최소 1개, 최대 1000개 범위 내에서 발생합니다
- 알림 발생률은 분당 최대 100개를 초과하지 않습니다

## Dependencies

### Prerequisite Features
- **001-realtime-fds**: 기본 마이크로서비스 인프라(transaction-generator, fraud-detector, alert-service, websocket-gateway, frontend-dashboard)가 구축되어 있어야 합니다

### External Dependencies
- 없음 (내부 시스템만 사용)

---

**Note**: 이 명세서는 `/speckit.specify` 명령을 통해 생성되었으며, 다음 단계로 `/speckit.plan` 또는 `/speckit.clarify` 명령을 실행할 수 있습니다.
