apiVersion: apps/v1
kind: Deployment
metadata:
  name: alert-dashboard
  namespace: realfds
spec:
  replicas: 2
  selector:
    matchLabels:
      app: alert-dashboard
  template:
    metadata:
      labels:
        app: alert-dashboard
    spec:
      containers:
      - name: alert-dashboard
        image: ghcr.io/jihosong-sjh/realfds-alert-dashboard-backend:latest
        ports:
        - containerPort: 8084
          name: http
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "prod"
        - name: R2DBC_URL
          valueFrom:
            secretKeyRef:
              name: realfds-secrets
              key: R2DBC_URL
        - name: R2DBC_USERNAME
          valueFrom:
            secretKeyRef:
              name: realfds-secrets
              key: R2DBC_USERNAME
        - name: R2DBC_PASSWORD
          valueFrom:
            secretKeyRef:
              name: realfds-secrets
              key: R2DBC_PASSWORD
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8084
          initialDelaySeconds: 30
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8084
          initialDelaySeconds: 60
          periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: alert-dashboard
  namespace: realfds
spec:
  type: ClusterIP
  selector:
    app: alert-dashboard
  ports:
  - port: 8084
    targetPort: 8084
    name: http
